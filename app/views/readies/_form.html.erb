<%= form_with(model: ready) do |form| %>
  <% if ready.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ready.errors.count, "error") %> prohibited this ready from being saved:</h2>

      <ul>
        <% ready.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :bracket_id, style: "display: block" %>
    <%= form.text_field :bracket_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>
  
  <div>
    <%= form.button "Next &rarr;".html_safe, {type: "submit", class: "navigation"} %>
  </div>
<% end %>
